module.exports = {
  preset: "ts-jest", // Use ts-jest to compile TypeScript for Jest
  testEnvironment: "node", // Or 'jsdom' if you're testing browser-like code
  testMatch: ["**/*.test.ts"], // Adjust the pattern if needed
};
